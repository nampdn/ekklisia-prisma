setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
  config:
    DATABASE_URL: DATABASE_URL
run:
  web: /bin/sh -c echo "DATABASE_URL=${DATABASE_URL}" > .env && node dist/server
