setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
  config:
    DATABASE_URL: DATABASE_URL
release:
  command: ./scripts/release.sh
  image: web
run:
  web: node dist/server
