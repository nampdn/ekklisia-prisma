setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
  config:
    DATABASE_URL: DATABASE_URL
release:
  image: web
run:
  web: node dist/server
  config:
    DATABASE_UR: DATABASE_URL
