# Migration `20191223084200-initial-ekklisia`

This migration has been generated by nampdn at 12/23/2019, 8:42:00 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "ekklisia"."User" (
  "email" text NOT NULL DEFAULT '' ,
  "id" text NOT NULL  ,
  "name" text   ,
  "password" text NOT NULL DEFAULT '' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "ekklisia"."Post" (
  "content" text   ,
  "createdAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "id" text NOT NULL  ,
  "published" boolean NOT NULL DEFAULT false ,
  "title" text NOT NULL DEFAULT '' ,
  "updatedAt" timestamp(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

CREATE TABLE "ekklisia"."Role" (
  "id" text NOT NULL  ,
  "name" text NOT NULL DEFAULT '' ,
  "roleCode" integer NOT NULL DEFAULT 0 ,
  PRIMARY KEY ("id")
);

CREATE TABLE "ekklisia"."Profile" (
  "birthday" timestamp(3)   ,
  "dayOfBirth" integer   ,
  "email" text   ,
  "facebookId" text   ,
  "fullName" text NOT NULL DEFAULT '' ,
  "gender" text NOT NULL DEFAULT '' ,
  "id" text NOT NULL  ,
  "joinDate" timestamp(3)   ,
  "monthOfBirth" integer   ,
  "oldId" text   ,
  "phoneNumber" text   ,
  "yearOfBirth" integer   ,
  PRIMARY KEY ("id")
);

CREATE TABLE "ekklisia"."_RoleToUser" (
  "A" text NOT NULL  REFERENCES "ekklisia"."Role"("id") ON DELETE CASCADE,
  "B" text NOT NULL  REFERENCES "ekklisia"."User"("id") ON DELETE CASCADE
);

ALTER TABLE "ekklisia"."Post" ADD COLUMN "author" text   REFERENCES "ekklisia"."User"("id") ON DELETE SET NULL;

ALTER TABLE "ekklisia"."Profile" ADD COLUMN "users" text NOT NULL  REFERENCES "ekklisia"."User"("id") ON DELETE RESTRICT;

CREATE UNIQUE INDEX "User.email" ON "ekklisia"."User"("email")

CREATE UNIQUE INDEX "Profile_users" ON "ekklisia"."Profile"("users")

CREATE UNIQUE INDEX "_RoleToUser_AB_unique" ON "ekklisia"."_RoleToUser"("A","B")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20191223084200-initial-ekklisia
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,51 @@
+generator photon {
+  provider = "photonjs"
+}
+
+datasource db {
+  provider = "postgresql"
+  url      = "postgresql://postgres:3bb98affe3c6756d2314c917cb4c8aed@us-seattle-m1.nodes.vgm.me:9433/prisma2?schema=ekklisia"
+}
+
+model User {
+  id       String  @default(cuid()) @id
+  email    String  @unique
+  password String
+  name     String?
+  posts    Post[]
+  roles    Role[]
+  profile  Profile
+}
+
+model Post {
+  id        String   @default(cuid()) @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  published Boolean  @default(false)
+  title     String
+  content   String?
+  author    User?
+}
+
+model Role {
+  id       String @default(cuid()) @id
+  name     String
+  roleCode Int
+  users    User[]
+}
+
+model Profile {
+  id           String    @default(cuid()) @id
+  fullName     String
+  gender       String
+  oldId        String?
+  email        String?
+  facebookId   String?
+  phoneNumber  String?
+  birthday     DateTime?
+  joinDate     DateTime?
+  dayOfBirth   Int?
+  monthOfBirth Int?
+  yearOfBirth  Int?
+  users        User
+}
```


