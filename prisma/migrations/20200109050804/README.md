# Migration `20200109050804`

This migration has been generated by nampdn at 1/9/2020, 5:08:04 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."GroupEnrollment" (
  "createdAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "group" text  NOT NULL  REFERENCES "public"."Group"("id") ON DELETE RESTRICT,
  "id" text  NOT NULL  ,
  "profile" text  NOT NULL  REFERENCES "public"."Profile"("id") ON DELETE RESTRICT,
  "reason" text  NOT NULL DEFAULT '' ,
  "updatedAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

ALTER TABLE "public"."Group" ADD COLUMN "createdAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00' ,
ADD COLUMN "endAt" timestamp(3)    ,
ADD COLUMN "stage" text  NOT NULL DEFAULT '' ,
ADD COLUMN "startAt" timestamp(3)    ,
ADD COLUMN "updatedAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00' ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200109043458-add-schedule-and-attendance..20200109050804
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model Post {
   id        String   @default(cuid()) @id
@@ -45,16 +45,30 @@
   updatedAt    DateTime  @updatedAt
 }
 model Group {
-  id      String    @default(cuid()) @id
-  name    String
-  slug    String?   @unique
-  year    Int?
-  leader  Profile?  @relation(name: "GroupLeader")
-  members Profile[] @relation(name: "GroupMembers")
+  id        String    @default(cuid()) @id
+  name      String
+  slug      String?   @unique
+  year      Int?
+  stage     String
+  leader    Profile?  @relation(name: "GroupLeader")
+  members   Profile[] @relation(name: "GroupMembers")
+  startAt   DateTime?
+  endAt     DateTime?
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
 }
+model GroupEnrollment {
+  id        String   @default(cuid()) @id
+  group     Group
+  profile   Profile
+  reason    String
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
+
 model Org {
   id         String     @default(cuid()) @id
   name       String
   groups     Group[]
```


