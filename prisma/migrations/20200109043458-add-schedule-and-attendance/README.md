# Migration `20200109043458-add-schedule-and-attendance`

This migration has been generated by nampdn at 1/9/2020, 4:34:58 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Schedule" (
  "activity" text  NOT NULL  REFERENCES "public"."Activity"("id") ON DELETE RESTRICT,
  "date" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "id" text  NOT NULL  ,
  PRIMARY KEY ("id")
);

CREATE TABLE "public"."Attendance" (
  "createdAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  "id" text  NOT NULL  ,
  "member" text  NOT NULL  REFERENCES "public"."Profile"("id") ON DELETE RESTRICT,
  "present" boolean  NOT NULL DEFAULT false ,
  "status" text  NOT NULL DEFAULT '' ,
  "updatedAt" timestamp(3)  NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY ("id")
);

ALTER TABLE "public"."Attendance" ADD COLUMN "schedule" text  NOT NULL  REFERENCES "public"."Schedule"("id") ON DELETE RESTRICT;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200106064512-add-created-at-and-updated-at-for-profile..20200109043458-add-schedule-and-attendance
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 model Post {
   id        String   @default(cuid()) @id
@@ -65,5 +65,21 @@
   id   String @default(cuid()) @id
   name String
   type String
   org  Org
+}
+
+model Schedule {
+  id       String   @default(cuid()) @id
+  activity Activity @relation(name: "ScheduleActivity")
+  date     DateTime
+}
+
+model Attendance {
+  id        String   @default(cuid()) @id
+  schedule  Schedule @relation(name: "AttendanceSchedule")
+  member    Profile  @relation(name: "AttendanceProfile")
+  status    String
+  present   Boolean
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
 }
```


